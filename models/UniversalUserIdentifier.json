{
  "title": "UniversalUserIdentifier",
  "type": "object",
  "description": "An identifier string & object for all Kelp-anything related profiles, used to aim back to a global stem account.",
  "properties": {
    "record": {
      "type": "number",
      "example": 18233,
      "default": 0,
      "description": "A schema-unique identifier for internal databases as well as `POST` object referencing methods.",
      "readOnly": true
    },
    "userName": {
      "type": "string",
      "description": "A changeable human readable identifier that is exclusive (while they have it) to a user, that can be used to search for them on diffrent Kelp sites, friend them, etc.\n\n*__Note:__ This defaults to their identifier on whatever platform they created the account from/with. It's only unique to them while they have it.*\n\n*__Examples:__\n\"maccaroon2910\" (\"Maccaroon#2910\") or \n\"johndoe\" (\"johndoe@gmail.com\")*",
      "example": "PinkFlower1023"
    },
    "security": {
      "type": "object",
      "required": [
        "hasAccess"
      ],
      "properties": {
        "hasAccess": {
          "type": "boolean",
          "default": false,
          "readOnly": true
        },
        "secured": {
          "type": "object",
          "properties": {
            "accessHistory": {
              "type": "string"
            },
            "personalPreferences": {
              "type": "string"
            },
            "currentDevices": {
              "type": "array",
              "items": {
                "$ref": "./CurrentlySavedDevice.json"
              },
              "readOnly": true
            },
            "2faOptions": {
              "type": "array",
              "items": {
                "$ref": "./2faDestinationInsert.json"
              },
              "readOnly": true
            }
          }
        }
      },
      "readOnly": true
    }
  },
  "required": [
    "record",
    "userName",
    "security"
  ],
  "x-tags": [
    "v1/global"
  ]
}