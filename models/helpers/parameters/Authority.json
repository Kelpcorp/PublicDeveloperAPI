{
  "name": "Authority",
  "in": "header",
  "required": false,
  "schema": {
    "type": [
      "array",
      "string"
    ],
    "format": "bearer",
    "items": {
      "type": "string",
      "format": "bearer"
    }
  },
  "description": "Optionally, one or more user authorized scoped bearer tokens. These allow developer applications to edit and acess privated or secured information from or to a users profile, settings and content."
}