{
  "name": "client",
  "in": "query",
  "required": true,
  "schema": {
    "type": "string",
    "format": "uuid",
    "minLength": 32,
    "maxLength": 36
  },
  "description": "Your developer applications public client identifier, normally formatted with UUID.\n\n*__Note:__ You do not need to include the dash separators in the UUID.*"
}