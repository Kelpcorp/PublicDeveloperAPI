{
  "type": "boolean",
  "title": "Server Error Status",
  "description": "Responds with boolean depending on if the backend servers ran into an error with the parameters or permissions checks.",
  "readOnly": true,
  "examples": [
    false
  ]
}