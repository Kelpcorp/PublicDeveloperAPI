{
  "title": "CurrentlySavedDevice",
  "type": "object",
  "properties": {
    "record": {
      "type": "integer",
      "description": "A schema-unique identifier for internal databases as well as `POST` object referencing methods.",
      "example": 23188,
      "readOnly": true
    }
  },
  "required": [
    "record"
  ],
  "x-tags": [
    "v1/global/2fa"
  ]
}